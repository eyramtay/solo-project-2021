import React from 'react';
import { useSelector } from 'react-redux';

function RandomizeEntires() {
    // const user = useSelector((store) => store.user);
    return (
        <>
        <h2>Randomize Page Under Construction</h2>
        </>
    )
}


export default RandomizeEntires;